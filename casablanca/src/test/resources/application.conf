taskManager { 
	db = testTaskDb
	taskTableName = testTasks
	statusQueueSize = 100 
}

testTaskDb {   
    driver = "org.hsqldb.jdbc.JDBCDriver"
	connection = "jdbc:hsqldb:testTasks"
	user = "SA"
	pass = ""	
	deleteSql = "DROP TABLE testTasks"
	createSql = "CREATE TABLE IF NOT EXISTS testTasks (taskId VARCHAR(256), createTime BIGINT, taskType VARCHAR(256), status INTEGER, attemptCount INTEGER, scheduleTime BIGINT, strPayload VARCHAR(256), intValue INTEGER)"
	
}

testDb {   
    driver = "org.hsqldb.jdbc.JDBCDriver"
	connection = "jdbc:hsqldb:test"
	user = "SA"
	pass = ""	
	deleteSql = "DROP TABLE test"
	createSql = "CREATE TABLE IF NOT EXISTS test (id INTEGER, strId VARCHAR(256), createTime BIGINT, intVal INTEGER)"
	
}